home.html and functions.js can be used to see how to interact with the php files. Is not necessary to keep.

db_functions.php contain all functions that interact with the database. This file is not called directly by frontend.

To find a product by its ID, call the file findProdByID.php and pass the products ID.

To find products using a search term, pass the search term to findProdsBySearch.php

